import "system/fs";
from "std" import "nv";

fun main() {
    let files = fs.open("name.txt")
    files.append("""
    //Hello world
    """)
    fs.mkdir("name",isexisted)
    nv.echoln(fs.wd()) //urn path of working Directory
}